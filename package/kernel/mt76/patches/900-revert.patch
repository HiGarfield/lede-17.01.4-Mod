diff --git a/dma.c b/dma.c
index 67fd507..77cefcd 100644
--- a/dma.c
+++ b/dma.c
@@ -563,8 +563,10 @@ mt76_dma_rx_poll(struct napi_struct *napi, int budget)
 
 	rcu_read_unlock();
 
-	if (done < budget && napi_complete(napi))
+	if (done < budget) {
+		napi_complete(napi);
 		dev->drv->rx_poll_complete(dev, qid);
+	}
 
 	return done;
 }
